package com.demowebshop.fw;

import org.openqa.selenium.WebDriver;

public class UserRegAndLogin extends BaseHelper{

  public UserRegAndLogin(WebDriver driver) {
    super(driver);
  }

  //here will be all methods with page Login and Registration
}
